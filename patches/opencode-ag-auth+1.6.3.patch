diff --git a/node_modules/opencode-ag-auth/dist/src/antigravity/oauth.js b/node_modules/opencode-ag-auth/dist/src/antigravity/oauth.js
index 9324176..7c73469 100644
--- a/node_modules/opencode-ag-auth/dist/src/antigravity/oauth.js
+++ b/node_modules/opencode-ag-auth/dist/src/antigravity/oauth.js
@@ -1,8 +1,12 @@
 import { generatePKCE } from "@openauthjs/openauth/pkce";
+import * as crypto from "node:crypto";
 import { ANTIGRAVITY_CLIENT_ID, ANTIGRAVITY_CLIENT_SECRET, ANTIGRAVITY_REDIRECT_URI, ANTIGRAVITY_SCOPES, ANTIGRAVITY_ENDPOINT_FALLBACKS, ANTIGRAVITY_LOAD_ENDPOINTS, getAntigravityHeaders, GEMINI_CLI_HEADERS, } from "../constants";
 import { createLogger } from "../plugin/logger";
 import { calculateTokenExpiry } from "../plugin/auth";
 const log = createLogger("oauth");
+
+const pendingStates = new Map();
+
 /**
  * Encode an object into a URL-safe base64 string.
  */
@@ -20,6 +24,10 @@ function decodeState(state) {
     if (typeof parsed.verifier !== "string") {
         throw new Error("Missing PKCE verifier in state");
     }
+    if (typeof parsed.nonce !== "string" || !pendingStates.has(parsed.nonce)) {
+        throw new Error("Invalid, expired, or replayed OAuth state (CSRF protection)");
+    }
+    pendingStates.delete(parsed.nonce);
     return {
         verifier: parsed.verifier,
         projectId: typeof parsed.projectId === "string" ? parsed.projectId : "",
@@ -30,6 +38,18 @@ function decodeState(state) {
  */
 export async function authorizeAntigravity(projectId = "") {
     const pkce = (await generatePKCE());
+    
+    // Cleanup old nonces (15 minute TTL)
+    const now = Date.now();
+    for (const [key, timestamp] of pendingStates.entries()) {
+        if (now - timestamp > 15 * 60 * 1000) {
+            pendingStates.delete(key);
+        }
+    }
+    
+    const nonce = crypto.randomUUID();
+    pendingStates.set(nonce, now);
+    
     const url = new URL("https://accounts.google.com/o/oauth2/v2/auth");
     url.searchParams.set("client_id", ANTIGRAVITY_CLIENT_ID);
     url.searchParams.set("response_type", "code");
@@ -37,7 +57,7 @@ export async function authorizeAntigravity(projectId = "") {
     url.searchParams.set("scope", ANTIGRAVITY_SCOPES.join(" "));
     url.searchParams.set("code_challenge", pkce.challenge);
     url.searchParams.set("code_challenge_method", "S256");
-    url.searchParams.set("state", encodeState({ verifier: pkce.verifier, projectId: projectId || "" }));
+    url.searchParams.set("state", encodeState({ verifier: pkce.verifier, projectId: projectId || "", nonce }));
     url.searchParams.set("access_type", "offline");
     url.searchParams.set("prompt", "consent");
     return {
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin.js b/node_modules/opencode-ag-auth/dist/src/plugin.js
index 9f86f17..bdef800 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin.js
@@ -5,7 +5,7 @@ import { authorizeAntigravity, exchangeAntigravity } from "./antigravity/oauth";
 import { accessTokenExpired, isOAuthAuth, parseRefreshParts, formatRefreshParts, } from "./plugin/auth";
 import { promptAddAnotherAccount, promptLoginMode, promptProjectId, pause, } from "./plugin/cli";
 import { ensureProjectContext } from "./plugin/project";
-import { startAntigravityDebugRequest, logAntigravityDebugResponse, logAccountContext, logRateLimitEvent, logRateLimitSnapshot, logResponseBody, logModelFamily, isDebugEnabled, getLogFilePath, initializeDebug, } from "./plugin/debug";
+import { startAntigravityDebugRequest, logAntigravityDebugResponse, logAccountContext, logRateLimitEvent, logRateLimitSnapshot, logResponseBody, logModelFamily, isDebugEnabled, getLogFilePath, initializeDebug, recordMetric, logMetricsSnapshot } from "./plugin/debug";
 import { buildThinkingWarmupBody, isGenerativeLanguageRequest, prepareAntigravityRequest, transformAntigravityResponse, } from "./plugin/request";
 import { resolveModelWithTier } from "./plugin/transform/model-resolver";
 import { isEmptyResponseBody, createSyntheticErrorResponse, } from "./plugin/request-helpers";
@@ -21,7 +21,7 @@ import { checkAccountsQuota } from "./plugin/quota";
 import { initDiskSignatureCache } from "./plugin/cache";
 import { createProactiveRefreshQueue, } from "./plugin/refresh-queue";
 import { initLogger, createLogger } from "./plugin/logger";
-import { initHealthTracker, getHealthTracker, initTokenTracker, getTokenTracker, } from "./plugin/rotation";
+import { initHealthTracker, getHealthTracker, initTokenTracker, getTokenTracker, getCircuitBreaker } from "./plugin/rotation";
 import { initAntigravityVersion } from "./plugin/version";
 import { executeSearch } from "./plugin/search";
 import { configureProxy } from "./plugin/proxy";
@@ -195,31 +195,27 @@ function shouldSkipLocalServer() {
     return isWSL2() || isRemoteEnvironment();
 }
 async function openBrowser(url) {
-    try {
-        if (process.platform === "darwin") {
-            exec(`open "${url}"`);
-            return true;
-        }
-        if (process.platform === "win32") {
-            exec(`start "" "${url}"`);
+    const run = (command) => new Promise((resolve) => {
+        exec(command, (error) => {
+            resolve(!error);
+        });
+    });
+    if (process.platform === "darwin") {
+        return run(`open "${url}"`);
+    }
+    if (process.platform === "win32") {
+        return run(`start "" "${url}"`);
+    }
+    if (isWSL()) {
+        const openedWithWslview = await run(`wslview "${url}"`);
+        if (openedWithWslview) {
             return true;
         }
-        if (isWSL()) {
-            try {
-                exec(`wslview "${url}"`);
-                return true;
-            }
-            catch { }
-        }
-        if (!process.env.DISPLAY && !process.env.WAYLAND_DISPLAY) {
-            return false;
-        }
-        exec(`xdg-open "${url}"`);
-        return true;
     }
-    catch {
+    if (!process.env.DISPLAY && !process.env.WAYLAND_DISPLAY) {
         return false;
     }
+    return run(`xdg-open "${url}"`);
 }
 function decodeEscapedText(input) {
     return input
@@ -368,7 +364,15 @@ function extractVerificationErrorDetails(bodyText) {
         verifyUrl: selectBestVerificationUrl([...verificationUrls]),
     };
 }
+function isNonBlockingVerificationProjectError(message) {
+    const value = (message || "").toLowerCase();
+    return value.includes("invalid project resource name projects/") ||
+        value.includes("serviceusage.services.use") ||
+        value.includes("caller does not have required permission to use project") ||
+        value.includes("permission denied on resource project");
+}
 async function verifyAccountAccess(account, client, providerId) {
+    const accountLabel = account.email || "unknown-account";
     const parsed = parseRefreshParts(account.refreshToken);
     if (!parsed.refreshToken) {
         return {
@@ -391,6 +395,10 @@ async function verifyAccountAccess(account, client, providerId) {
         refreshedAuth = await refreshAccessToken(auth, client, providerId);
     }
     catch (error) {
+        log.debug("verify-account-refresh-failed", {
+            account: accountLabel,
+            error: String(error),
+        });
         if (error instanceof AntigravityTokenRefreshError) {
             return { status: "error", message: error.message };
         }
@@ -410,14 +418,29 @@ async function verifyAccountAccess(account, client, providerId) {
         account.managedProjectId ??
         account.projectId ??
         ANTIGRAVITY_DEFAULT_PROJECT_ID;
+    const nativeHeaders = getAntigravityHeaders();
+    if (account.fingerprint?.userAgent) {
+        nativeHeaders["User-Agent"] = account.fingerprint.userAgent;
+    }
+    if (account.fingerprint?.apiClient) {
+        nativeHeaders["X-Goog-Api-Client"] = account.fingerprint.apiClient;
+    }
+    if (account.fingerprint?.clientMetadata &&
+        typeof account.fingerprint.clientMetadata === "object") {
+        try {
+            nativeHeaders["Client-Metadata"] = JSON.stringify(account.fingerprint.clientMetadata);
+        }
+        catch {
+            // Keep default metadata if fingerprint metadata cannot be serialized.
+        }
+    }
     const headers = {
-        ...getAntigravityHeaders(),
+        ...nativeHeaders,
         Authorization: `Bearer ${refreshedAuth.access}`,
         "Content-Type": "application/json",
     };
-    if (projectId) {
-        headers["x-goog-user-project"] = projectId;
-    }
+    // Verification should stay project-agnostic. Injecting x-goog-user-project here can
+    // fail with IAM errors (serviceusage.services.use) even when account verification is valid.
     const requestBody = {
         model: "gemini-3-flash",
         request: {
@@ -426,29 +449,66 @@ async function verifyAccountAccess(account, client, providerId) {
             generationConfig: { maxOutputTokens: 1, temperature: 0 },
         },
     };
-    const controller = new AbortController();
-    const timeoutId = setTimeout(() => controller.abort(), 20000);
+    const endpoints = Array.from(new Set([
+        ANTIGRAVITY_ENDPOINT_PROD,
+        ...ANTIGRAVITY_ENDPOINT_FALLBACKS,
+    ]));
     let response;
-    try {
-        response = await fetch(`${ANTIGRAVITY_ENDPOINT_PROD}/v1internal:streamGenerateContent?alt=sse`, {
-            method: "POST",
-            headers,
-            body: JSON.stringify(requestBody),
-            signal: controller.signal,
-        });
+    let lastError;
+    for (const endpoint of endpoints) {
+        const controller = new AbortController();
+        const timeoutId = setTimeout(() => controller.abort(), 20000);
+        try {
+            log.debug("verify-account-attempt", {
+                account: accountLabel,
+                endpoint,
+            });
+            response = await fetch(`${endpoint}/v1internal:streamGenerateContent?alt=sse`, {
+                method: "POST",
+                headers,
+                body: JSON.stringify(requestBody),
+                signal: controller.signal,
+            });
+            if (response.status >= 500 && endpoint !== endpoints[endpoints.length - 1]) {
+                log.debug("verify-account-endpoint-retry", {
+                    account: accountLabel,
+                    endpoint,
+                    status: response.status,
+                });
+                response = undefined;
+                continue;
+            }
+            break;
+        }
+        catch (error) {
+            lastError = error;
+            log.debug("verify-account-attempt-failed", {
+                account: accountLabel,
+                endpoint,
+                error: String(error),
+            });
+            if (error instanceof Error && error.name === "AbortError") {
+                continue;
+            }
+            continue;
+        }
+        finally {
+            clearTimeout(timeoutId);
+        }
     }
-    catch (error) {
-        if (error instanceof Error && error.name === "AbortError") {
+    if (!response) {
+        log.debug("verify-account-no-response", {
+            account: accountLabel,
+            error: String(lastError ?? "No endpoint available"),
+        });
+        if (lastError instanceof Error && lastError.name === "AbortError") {
             return { status: "error", message: "Verification check timed out." };
         }
         return {
             status: "error",
-            message: `Verification check failed: ${String(error)}`,
+            message: `Verification check failed: ${String(lastError ?? "No endpoint available")}`,
         };
     }
-    finally {
-        clearTimeout(timeoutId);
-    }
     let responseBody = "";
     try {
         responseBody = await response.text();
@@ -457,18 +517,38 @@ async function verifyAccountAccess(account, client, providerId) {
         responseBody = "";
     }
     if (response.ok) {
+        log.debug("verify-account-ok", {
+            account: accountLabel,
+            status: response.status,
+        });
         return { status: "ok", message: "Account verification check passed." };
     }
     const extracted = extractVerificationErrorDetails(responseBody);
     if (response.status === 403 && extracted.validationRequired) {
+        log.debug("verify-account-blocked", {
+            account: accountLabel,
+            status: response.status,
+            hasVerifyUrl: Boolean(extracted.verifyUrl),
+        });
         return {
             status: "blocked",
             message: extracted.message ?? "Google requires additional account verification.",
             verifyUrl: extracted.verifyUrl,
         };
     }
+    log.debug("verify-account-error", {
+        account: accountLabel,
+        status: response.status,
+        statusText: response.statusText,
+    });
     const fallbackMessage = extracted.message ??
         `Request failed (${response.status} ${response.statusText}).`;
+    if (isNonBlockingVerificationProjectError(fallbackMessage)) {
+        return {
+            status: "ok",
+            message: `Account token is valid (verification passed). Non-blocking project check issue: ${fallbackMessage}`,
+        };
+    }
     return {
         status: "error",
         message: fallbackMessage,
@@ -589,6 +669,9 @@ function getStateFromAuthorizationUrl(authorizationUrl) {
     }
 }
 function extractOAuthCallbackParams(url) {
+    if (url.pathname !== "/oauth-callback" && url.pathname !== "/") {
+        return null; // Ignore requests to favicon.ico, etc.
+    }
     const code = url.searchParams.get("code");
     const state = url.searchParams.get("state");
     if (!code || !state) {
@@ -599,27 +682,29 @@ function extractOAuthCallbackParams(url) {
 function parseOAuthCallbackInput(value, fallbackState) {
     const trimmed = value.trim();
     if (!trimmed) {
-        return { error: "Missing authorization code" };
+        return { error: "Missing authorization input" };
     }
     try {
         const url = new URL(trimmed);
+        // Basic origin/path validation (expecting localhost /oauth-callback or similar)
+        if (url.protocol !== "http:" && url.protocol !== "https:") {
+            return { error: "Invalid callback URL protocol" };
+        }
         const code = url.searchParams.get("code");
         const state = url.searchParams.get("state") ?? fallbackState;
         if (!code) {
             return { error: "Missing code in callback URL" };
         }
         if (!state) {
-            return { error: "Missing state in callback URL" };
+            return { error: "Missing state in callback URL. Please paste the full URL." };
         }
         return { code, state };
     }
     catch {
-        if (!fallbackState) {
-            return {
-                error: "Missing state. Paste the full redirect URL instead of only the code.",
-            };
-        }
-        return { code: trimmed, state: fallbackState };
+        // Reject raw code inputs to enforce state validation
+        return {
+            error: "Invalid input. Please paste the FULL redirect URL (starting with http://localhost) instead of only the code.",
+        };
     }
 }
 async function promptManualOAuthInput(fallbackState) {
@@ -1370,7 +1455,18 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                             // Use AccountManager method which properly checks for disabled/cooling-down accounts
                             return accountManager.hasOtherAccountWithAntigravityAvailable(currentAccount.index, family, model);
                         };
+                        const requestStartTime = Date.now();
+                        const MAX_WALL_CLOCK_MS = 5 * 60 * 1000; // 5 minutes max total duration per request
+                        let routingLoopCount = 0;
+                        const MAX_ROUTING_ITERATIONS = 20; // Max account switch/retry cycles
                         while (true) {
+                            routingLoopCount++;
+                            if (routingLoopCount > MAX_ROUTING_ITERATIONS) {
+                                throw new Error(`Antigravity Auth: Request failed after ${MAX_ROUTING_ITERATIONS} routing attempts (infinite loop protection).`);
+                            }
+                            if (Date.now() - requestStartTime > MAX_WALL_CLOCK_MS) {
+                                throw new Error(`Antigravity Auth: Request timed out after 5 minutes of retries and wait times.`);
+                            }
                             // Check for abort at the start of each iteration
                             checkAborted();
                             const accountCount = accountManager.getAccountCount();
@@ -1712,6 +1808,13 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                         lastEndpointIndex = i;
                                     }
                                     const currentEndpoint = ANTIGRAVITY_ENDPOINT_FALLBACKS[i];
+                                    
+                                    // Skip if circuit breaker says OPEN (except if it's the last fallback)
+                                    if (!getCircuitBreaker().canUseEndpoint(currentEndpoint) && i < ANTIGRAVITY_ENDPOINT_FALLBACKS.length - 1) {
+                                        pushDebug(`Skipping endpoint ${currentEndpoint} (Circuit Breaker OPEN)`);
+                                        continue;
+                                    }
+
                                     // Skip sandbox endpoints for Gemini CLI models - they only work with Antigravity quota
                                     // Gemini CLI models must use production endpoint (cloudcode-pa.googleapis.com)
                                     if (headerStyle === "gemini-cli" &&
@@ -1769,7 +1872,9 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                         if (config.account_selection_strategy === "hybrid") {
                                             tokenConsumed = getTokenTracker().consume(account.index);
                                         }
+                                        const requestStartMs = Date.now();
                                         let response = await fetch(prepared.request, prepared.init);
+                                        const latencyMs = Date.now() - requestStartMs;
                                         pushDebug(`status=${response.status} ${response.statusText}`);
                                         // Sonnet 4.6 Thinking fallback:
                                         // Some Antigravity backends no longer expose claude-sonnet-4-6-thinking.
@@ -1858,6 +1963,7 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             if (bodyInfo.reason) {
                                                 pushDebug(`429 reason=${bodyInfo.reason}`);
                                             }
+                                            recordMetric('rateLimit');
                                             logRateLimitEvent(account.index, account.email, family, response.status, effectiveDelayMs, bodyInfo);
                                             await logResponseBody(debugContext, response, 429);
                                             getHealthTracker().recordRateLimit(account.index);
@@ -1904,7 +2010,7 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                                         await showToast(`Rate limited again. Switching account in 5s...`, "warning");
                                                         await sleep(SWITCH_ACCOUNT_DELAY_MS, abortSignal);
                                                         shouldSwitchAccount = true;
-                                                        break;
+                                    break;
                                                     }
                                                     // All accounts exhausted for Antigravity on THIS model.
                                                     // Before falling back to gemini-cli, check if it's the last option (automatic fallback)
@@ -2032,11 +2138,64 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                                 shouldSwitchAccount = true;
                                                 break;
                                             }
+                                            const bodyLower = errorBodyText.toLowerCase();
+                                            const isTosDisabled = bodyLower.includes("violation of terms of service") ||
+                                                bodyLower.includes("disabled in this account") ||
+                                                bodyLower.includes("submit an appeal");
+                                            if (isTosDisabled) {
+                                                const label = account.email || `Account ${account.index + 1}`;
+                                                if (config.delete_account_on_403_tos) {
+                                                    const removed = accountManager.removeAccount(account);
+                                                    if (removed) {
+                                                        try {
+                                                            await accountManager.saveToDisk();
+                                                        }
+                                                        catch (persistError) {
+                                                            log.error("Failed to persist ToS-disabled account removal", {
+                                                                error: String(persistError),
+                                                            });
+                                                        }
+                                                    }
+                                                    await showToast(`⚠ ${label} removed after ToS disable (403).`, "warning");
+                                                    pushDebug(`tos-disabled: removed account ${account.index}`);
+                                                }
+                                                else {
+                                                    const cooldownMs = 24 * 60 * 60 * 1000;
+                                                    accountManager.setAccountEnabled(account.index, false);
+                                                    accountManager.markAccountCoolingDown(account, cooldownMs, "tos-disabled");
+                                                    accountManager.markRateLimited(account, cooldownMs, family, headerStyle, model);
+                                                    await showToast(`⚠ ${label} disabled after ToS disable (403).`, "warning");
+                                                    pushDebug(`tos-disabled: disabled account ${account.index}`);
+                                                }
+                                                getHealthTracker().recordFailure(account.index);
+                                                if (accountManager.getAccountCount() === 0) {
+                                                    try {
+                                                        await client.auth.set({
+                                                            path: { id: providerId },
+                                                            body: {
+                                                                type: "oauth",
+                                                                refresh: "",
+                                                                access: "",
+                                                                expires: 0,
+                                                            },
+                                                        });
+                                                    }
+                                                    catch (storeError) {
+                                                        log.error("Failed to clear stored Antigravity OAuth credentials", {
+                                                            error: String(storeError),
+                                                        });
+                                                    }
+                                                }
+                                                return createSyntheticErrorResponse(config.delete_account_on_403_tos
+                                                    ? "[Antigravity Error] Account disabled by Google for Terms of Service violation (403).\n\nThe account was removed locally. Please run `opencode auth login` and submit an appeal for that Google account before re-adding it."
+                                                    : "[Antigravity Error] Account disabled by Google for Terms of Service violation (403).\n\nThe account was disabled locally (not deleted). Please submit an appeal for that Google account before re-enabling it.", prepared.requestedModel);
+                                            }
                                         }
                                         const shouldRetryEndpoint = response.status === 403 ||
                                             response.status === 404 ||
                                             response.status >= 500;
                                         if (shouldRetryEndpoint) {
+                                            getCircuitBreaker().recordFailure(currentEndpoint);
                                             await logResponseBody(debugContext, response, response.status);
                                         }
                                         if (shouldRetryEndpoint &&
@@ -2059,9 +2218,13 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                         // Success or non-retryable error - return the response
                                         if (response.ok) {
                                             account.consecutiveFailures = 0;
-                                            getHealthTracker().recordSuccess(account.index);
+                                            recordMetric('success');
+                                            recordMetric('latency', latencyMs);
+                                            getHealthTracker().recordSuccess(account.index, latencyMs);
+                                            getCircuitBreaker().recordSuccess(currentEndpoint);
                                             accountManager.markAccountUsed(account.index);
                                             void triggerAsyncQuotaRefreshForAccount(accountManager, account.index, client, providerId, config.quota_refresh_interval_minutes);
+                                            logMetricsSnapshot();
                                         }
                                         logAntigravityDebugResponse(debugContext, response, {
                                             note: response.ok
@@ -2165,6 +2328,7 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             continue;
                                         }
                                         // All endpoints failed for this account - track failure and try next account
+                                        recordMetric('timeout');
                                         const { failures, shouldCooldown, cooldownMs } = trackAccountFailure(account.index);
                                         lastError =
                                             error instanceof Error
@@ -2522,6 +2686,9 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                     }
                                     if (menuResult.mode === "verify" ||
                                         menuResult.mode === "verify-all") {
+                                        if (process.env.OPENCODE_ANTIGRAVITY_VERIFY_DEBUG === "1") {
+                                            console.log(`\n[verify] mode=${menuResult.mode} index=${menuResult.verifyAccountIndex ?? "auto"}`);
+                                        }
                                         const verifyAll = menuResult.mode === "verify-all" ||
                                             menuResult.verifyAll === true;
                                         if (verifyAll) {
@@ -2592,12 +2759,22 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             else {
                                                 console.log("");
                                             }
+                                            await pause();
+                                            console.log("");
                                             continue;
                                         }
                                         let verifyAccountIndex = menuResult.verifyAccountIndex;
                                         if (verifyAccountIndex === undefined) {
-                                            verifyAccountIndex =
-                                                await promptAccountIndexForVerification(existingAccounts);
+                                            const firstVerificationRequiredIndex = existingStorage.accounts.findIndex((acc) => acc?.verificationRequired === true);
+                                            if (firstVerificationRequiredIndex >= 0) {
+                                                verifyAccountIndex = firstVerificationRequiredIndex;
+                                                const autoLabel = existingStorage.accounts[firstVerificationRequiredIndex]?.email || `Account ${firstVerificationRequiredIndex + 1}`;
+                                                console.log(`\nAuto-selecting ${autoLabel} because it is marked as needing verification.\n`);
+                                            }
+                                            else {
+                                                verifyAccountIndex =
+                                                    await promptAccountIndexForVerification(existingAccounts);
+                                            }
                                         }
                                         if (verifyAccountIndex === undefined) {
                                             console.log("\nVerification cancelled.\n");
@@ -2609,6 +2786,21 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             continue;
                                         }
                                         const label = account.email || `Account ${verifyAccountIndex + 1}`;
+                                        if (account.verificationRequired === true && account.verificationUrl) {
+                                            console.log(`\n${label} still requires Google verification.`);
+                                            console.log(`Verification URL:\n${account.verificationUrl}\n`);
+                                            const opened = await openBrowser(account.verificationUrl);
+                                            if (opened) {
+                                                console.log("Opened verification URL in your browser.");
+                                                console.log("Complete verification in browser, then run Verify again to re-check account status.\n");
+                                            }
+                                            else {
+                                                console.log("Could not open browser automatically. Open the URL manually, then run Verify again.\n");
+                                            }
+                                            await pause();
+                                            console.log("");
+                                            continue;
+                                        }
                                         console.log(`\nChecking verification status for ${label}...\n`);
                                         const verification = await verifyAccountAccess(account, client, providerId);
                                         if (verification.status === "ok") {
@@ -2623,6 +2815,8 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             else {
                                                 console.log(`✓ ${label} is ready for requests.\n`);
                                             }
+                                            await pause();
+                                            console.log("");
                                             continue;
                                         }
                                         if (verification.status === "blocked") {
@@ -2652,9 +2846,13 @@ export const createAntigravityPlugin = (providerId) => async ({ client, director
                                             else {
                                                 console.log("No verification URL was returned. Try re-authenticating this account.\n");
                                             }
+                                            await pause();
+                                            console.log("");
                                             continue;
                                         }
                                         console.log(`✗ ${label}: ${verification.message}\n`);
+                                        await pause();
+                                        console.log("");
                                         continue;
                                     }
                                     break;
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/accounts.js b/node_modules/opencode-ag-auth/dist/src/plugin/accounts.js
index 3e57c03..7f45f98 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/accounts.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/accounts.js
@@ -421,6 +421,8 @@ export class AccountManager {
                     index: acc.index,
                     lastUsed: acc.lastUsed,
                     healthScore: healthTracker.getScore(acc.index),
+                    successRate: healthTracker.scores.get(acc.index)?.successRate ?? 1.0,
+                    avgLatencyMs: healthTracker.scores.get(acc.index)?.avgLatencyMs ?? 2000,
                     isRateLimited: isRateLimitedForFamily(acc, family, model) ||
                         isOverSoftQuotaThreshold(acc, family, softQuotaThresholdPercent, softQuotaCacheTtlMs, model),
                     isCoolingDown: this.isAccountCoolingDown(acc),
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/config/loader.js b/node_modules/opencode-ag-auth/dist/src/plugin/config/loader.js
index db29782..9b5ed95 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/config/loader.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/config/loader.js
@@ -101,7 +101,15 @@ function mergeConfigs(base, override) {
  */
 function applyEnvOverrides(config) {
     const env = process.env;
-    return {
+    let soft_quota_override = config.soft_quota_threshold_percent;
+    if (env.OPENCODE_ANTIGRAVITY_SOFT_QUOTA_THRESHOLD_PERCENT) {
+        const val = parseInt(env.OPENCODE_ANTIGRAVITY_SOFT_QUOTA_THRESHOLD_PERCENT, 10);
+        if (Number.isFinite(val) && !isNaN(val)) {
+            soft_quota_override = val;
+        }
+    }
+
+    const merged = {
         ...config,
         // OPENCODE_ANTIGRAVITY_QUIET=1
         quiet_mode: env.OPENCODE_ANTIGRAVITY_QUIET === "1" ||
@@ -144,12 +152,25 @@ function applyEnvOverrides(config) {
             env.OPENCODE_ANTIGRAVITY_PID_OFFSET_ENABLED === "true"
             ? true
             : config.pid_offset_enabled,
+        // OPENCODE_ANTIGRAVITY_DELETE_ACCOUNT_ON_403_TOS=1
+        delete_account_on_403_tos: env.OPENCODE_ANTIGRAVITY_DELETE_ACCOUNT_ON_403_TOS === "1" ||
+            env.OPENCODE_ANTIGRAVITY_DELETE_ACCOUNT_ON_403_TOS === "true"
+            ? true
+            : env.OPENCODE_ANTIGRAVITY_DELETE_ACCOUNT_ON_403_TOS === "0" ||
+                env.OPENCODE_ANTIGRAVITY_DELETE_ACCOUNT_ON_403_TOS === "false"
+                ? false
+                : config.delete_account_on_403_tos,
         // OPENCODE_ANTIGRAVITY_SOFT_QUOTA_THRESHOLD_PERCENT=70
-        soft_quota_threshold_percent: process.env
-            .OPENCODE_ANTIGRAVITY_SOFT_QUOTA_THRESHOLD_PERCENT
-            ? parseInt(process.env.OPENCODE_ANTIGRAVITY_SOFT_QUOTA_THRESHOLD_PERCENT, 10)
-            : config.soft_quota_threshold_percent,
+        soft_quota_threshold_percent: soft_quota_override,
     };
+
+    const parsed = AntigravityConfigSchema.safeParse(merged);
+    if (!parsed.success) {
+        // Fallback to valid defaults for any invalid overridden keys
+        console.warn(`[Antigravity Auth] Invalid configuration after environment overrides: ${parsed.error.message}. Falling back to previous values.`);
+        return config; // Safest fallback: use pre-override valid config
+    }
+    return parsed.data;
 }
 // =============================================================================
 // Main Loader
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/config/models.js b/node_modules/opencode-ag-auth/dist/src/plugin/config/models.js
index 16cdb35..186c5b5 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/config/models.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/config/models.js
@@ -32,11 +32,27 @@ export const OPENCODE_MODEL_DEFINITIONS = {
             high: { thinkingLevel: "high" },
         },
     },
+    "gemini-3-flash": {
+        name: "Gemini 3 Flash (Antigravity)",
+        limit: { context: 1048576, output: 65536 },
+        modalities: DEFAULT_MODALITIES,
+        variants: {
+            minimal: { thinkingLevel: "minimal" },
+            low: { thinkingLevel: "low" },
+            medium: { thinkingLevel: "medium" },
+            high: { thinkingLevel: "high" },
+        },
+    },
     "antigravity-claude-sonnet-4-6": {
         name: "Claude Sonnet 4.6 (Antigravity)",
         limit: { context: 200000, output: 64000 },
         modalities: DEFAULT_MODALITIES,
     },
+    "claude-sonnet-4-6": {
+        name: "Claude Sonnet 4.6 (Antigravity)",
+        limit: { context: 200000, output: 64000 },
+        modalities: DEFAULT_MODALITIES,
+    },
     "antigravity-claude-sonnet-4-6-thinking": {
         name: "Claude Sonnet 4.6 Thinking (Antigravity)",
         limit: { context: 200000, output: 64000 },
@@ -46,6 +62,15 @@ export const OPENCODE_MODEL_DEFINITIONS = {
             max: { thinkingConfig: { thinkingBudget: 32768 } },
         },
     },
+    "claude-sonnet-4-6-thinking": {
+        name: "Claude Sonnet 4.6 Thinking (Antigravity)",
+        limit: { context: 200000, output: 64000 },
+        modalities: DEFAULT_MODALITIES,
+        variants: {
+            low: { thinkingConfig: { thinkingBudget: 8192 } },
+            max: { thinkingConfig: { thinkingBudget: 32768 } },
+        },
+    },
     "antigravity-claude-opus-4-6-thinking": {
         name: "Claude Opus 4.6 Thinking (Antigravity)",
         limit: { context: 200000, output: 64000 },
@@ -55,6 +80,15 @@ export const OPENCODE_MODEL_DEFINITIONS = {
             max: { thinkingConfig: { thinkingBudget: 32768 } },
         },
     },
+    "claude-opus-4-6-thinking": {
+        name: "Claude Opus 4.6 Thinking (Antigravity)",
+        limit: { context: 200000, output: 64000 },
+        modalities: DEFAULT_MODALITIES,
+        variants: {
+            low: { thinkingConfig: { thinkingBudget: 8192 } },
+            max: { thinkingConfig: { thinkingBudget: 32768 } },
+        },
+    },
     "gemini-2.5-flash": {
         name: "Gemini 2.5 Flash (Gemini CLI)",
         limit: { context: 1048576, output: 65536 },
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/config/schema.js b/node_modules/opencode-ag-auth/dist/src/plugin/config/schema.js
index dbc2316..ac1f6f5 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/config/schema.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/config/schema.js
@@ -275,6 +275,15 @@ export const AntigravityConfigSchema = z.object({
      * @default true
      */
     switch_on_first_rate_limit: z.boolean().default(true),
+    /**
+     * Remove account from local pool when Google returns 403 ToS-disabled response.
+     *
+     * When true: delete account from antigravity-accounts.json.
+     * When false: disable/cooldown account only.
+     *
+     * @default true
+     */
+    delete_account_on_403_tos: z.boolean().default(true),
     /**
      * Scheduling mode for rate limit behavior.
      *
@@ -396,7 +405,7 @@ export const DEFAULT_CONFIG = {
     debug_tui: false,
     keep_thinking: false,
     session_recovery: true,
-    auto_resume: true,
+    auto_resume: false,
     resume_text: "continue",
     empty_response_max_attempts: 4,
     empty_response_retry_delay_ms: 2000,
@@ -411,6 +420,7 @@ export const DEFAULT_CONFIG = {
     account_selection_strategy: "hybrid",
     pid_offset_enabled: false,
     switch_on_first_rate_limit: true,
+    delete_account_on_403_tos: true,
     scheduling_mode: "cache_first",
     max_cache_first_wait_seconds: 60,
     failure_ttl_seconds: 3600,
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/debug.js b/node_modules/opencode-ag-auth/dist/src/plugin/debug.js
index 342a5f0..463fa4c 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/debug.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/debug.js
@@ -211,7 +211,14 @@ function formatBodyPreview(body) {
         return truncateForLog(body);
     }
     if (body instanceof URLSearchParams) {
-        return truncateForLog(body.toString());
+        const redacted = new URLSearchParams(body);
+        for (const [key] of redacted.entries()) {
+            const lowerKey = key.toLowerCase();
+            if (lowerKey.includes("token") || lowerKey.includes("secret") || lowerKey.includes("password") || lowerKey === "code") {
+                redacted.set(key, "[redacted]");
+            }
+        }
+        return truncateForLog(redacted.toString());
     }
     if (typeof Blob !== "undefined" && body instanceof Blob) {
         return `[Blob size=${body.size}]`;
@@ -342,6 +349,29 @@ export function logModelFamily(url, extractedModel, family) {
         return;
     logDebug(`[ModelFamily] url=${url} model=${extractedModel ?? "unknown"} family=${family}`);
 }
+const internalMetrics = {
+    successCount: 0,
+    rateLimitCount: 0,
+    timeoutCount: 0,
+    totalLatencyMs: 0,
+    latencyCount: 0,
+};
+
+export function recordMetric(type, value = 1) {
+    if (type === 'success') internalMetrics.successCount += value;
+    if (type === 'rateLimit') internalMetrics.rateLimitCount += value;
+    if (type === 'timeout') internalMetrics.timeoutCount += value;
+    if (type === 'latency') {
+        internalMetrics.totalLatencyMs += value;
+        internalMetrics.latencyCount += 1;
+    }
+}
+
+export function logMetricsSnapshot() {
+    if (!getDebugState().debugEnabled) return;
+    const avgLatency = internalMetrics.latencyCount > 0 ? Math.round(internalMetrics.totalLatencyMs / internalMetrics.latencyCount) : 0;
+    logDebug(`[Metrics] Success=${internalMetrics.successCount} 429=${internalMetrics.rateLimitCount} Timeout=${internalMetrics.timeoutCount} AvgLatency=${avgLatency}ms`);
+}
 export function debugLogToFile(message) {
     if (!getDebugState().debugEnabled)
         return;
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/project.js b/node_modules/opencode-ag-auth/dist/src/plugin/project.js
index 684ef45..534b965 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/project.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/project.js
@@ -92,11 +92,20 @@ export async function loadManagedProject(accessToken, projectId) {
     const loadEndpoints = Array.from(new Set([...ANTIGRAVITY_LOAD_ENDPOINTS, ...ANTIGRAVITY_ENDPOINT_FALLBACKS]));
     for (const baseEndpoint of loadEndpoints) {
         try {
-            const response = await fetch(`${baseEndpoint}/v1internal:loadCodeAssist`, {
-                method: "POST",
-                headers: loadHeaders,
-                body: JSON.stringify(requestBody),
-            });
+            const controller = new AbortController();
+            const timeoutId = setTimeout(() => controller.abort(), 10000); // 10s timeout
+            let response;
+            try {
+                response = await fetch(`${baseEndpoint}/v1internal:loadCodeAssist`, {
+                    method: "POST",
+                    headers: loadHeaders,
+                    body: JSON.stringify(requestBody),
+                    signal: controller.signal,
+                });
+            } finally {
+                clearTimeout(timeoutId);
+            }
+            
             if (!response.ok) {
                 continue;
             }
@@ -121,15 +130,24 @@ export async function onboardManagedProject(accessToken, tierId, projectId, atte
     for (const baseEndpoint of ANTIGRAVITY_ENDPOINT_FALLBACKS) {
         for (let attempt = 0; attempt < attempts; attempt += 1) {
             try {
-                const response = await fetch(`${baseEndpoint}/v1internal:onboardUser`, {
-                    method: "POST",
-                    headers: {
-                        "Content-Type": "application/json",
-                        Authorization: `Bearer ${accessToken}`,
-                        ...getAntigravityHeaders(),
-                    },
-                    body: JSON.stringify(requestBody),
-                });
+                const controller = new AbortController();
+                const timeoutId = setTimeout(() => controller.abort(), 15000); // 15s timeout
+                let response;
+                try {
+                    response = await fetch(`${baseEndpoint}/v1internal:onboardUser`, {
+                        method: "POST",
+                        headers: {
+                            "Content-Type": "application/json",
+                            Authorization: `Bearer ${accessToken}`,
+                            ...getAntigravityHeaders(),
+                        },
+                        body: JSON.stringify(requestBody),
+                        signal: controller.signal,
+                    });
+                } finally {
+                    clearTimeout(timeoutId);
+                }
+                
                 if (!response.ok) {
                     break;
                 }
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/proxy.js b/node_modules/opencode-ag-auth/dist/src/plugin/proxy.js
index 1e9966f..c09be5f 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/proxy.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/proxy.js
@@ -10,10 +10,22 @@ export function configureProxy() {
         try {
             const dispatcher = new ProxyAgent(proxyUrl);
             setGlobalDispatcher(dispatcher);
-            log.info(`Proxy configured using ${proxyUrl}`);
+            let safeUrlStr = proxyUrl;
+            try {
+                const safeUrl = new URL(proxyUrl);
+                if (safeUrl.username) safeUrl.username = "***";
+                if (safeUrl.password) safeUrl.password = "***";
+                safeUrlStr = safeUrl.toString();
+            } catch (e) {
+                // Ignore parse errors, just mask the whole string if it contains @
+                if (safeUrlStr.includes("@")) {
+                    safeUrlStr = safeUrlStr.replace(/\/\/[^@]+@/, "//***:***@");
+                }
+            }
+            log.info(`Proxy configured using ${safeUrlStr}`);
         }
         catch (error) {
-            log.warn(`Failed to configure proxy: ${error}`);
+            log.warn(`Failed to configure proxy: invalid URL or dispatcher error`);
         }
     }
 }
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/request.js b/node_modules/opencode-ag-auth/dist/src/plugin/request.js
index 56b57f9..6d70ad0 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/request.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/request.js
@@ -1377,27 +1377,38 @@ export async function transformAntigravityResponse(response, streaming, debugCon
                     (errorMessage.includes("without") || errorMessage.includes("immediately after"))) {
                     headers.set("x-antigravity-context-error", "tool_pairing");
                 }
+
+                if (errorBody?.error?.details && Array.isArray(errorBody.error.details)) {
+                    const retryInfo = errorBody.error.details.find((detail) => detail['@type'] === 'type.googleapis.com/google.rpc.RetryInfo');
+                    if (retryInfo?.retryDelay) {
+                        const match = retryInfo.retryDelay.match(/^([\d.]+)s$/);
+                        if (match && match[1]) {
+                            const retrySeconds = parseFloat(match[1]);
+                            if (!isNaN(retrySeconds) && retrySeconds > 0) {
+                                const retryAfterSec = Math.ceil(retrySeconds).toString();
+                                const retryAfterMs = Math.ceil(retrySeconds * 1000).toString();
+                                headers.set('Retry-After', retryAfterSec);
+                                headers.set('retry-after-ms', retryAfterMs);
+                            }
+                        } else {
+                            // Fallback if retryDelay is somehow present but malformed
+                            const retrySeconds = parseFloat(retryInfo.retryDelay);
+                            if (!isNaN(retrySeconds) && retrySeconds > 0) {
+                                const retryAfterSec = Math.ceil(retrySeconds).toString();
+                                const retryAfterMs = Math.ceil(retrySeconds * 1000).toString();
+                                headers.set('Retry-After', retryAfterSec);
+                                headers.set('retry-after-ms', retryAfterMs);
+                            }
+                        }
+                    }
+                }
+
                 return new Response(JSON.stringify(errorBody), {
                     status: response.status,
                     statusText: response.statusText,
                     headers
                 });
             }
-            if (errorBody?.error?.details && Array.isArray(errorBody.error.details)) {
-                const retryInfo = errorBody.error.details.find((detail) => detail['@type'] === 'type.googleapis.com/google.rpc.RetryInfo');
-                if (retryInfo?.retryDelay) {
-                    const match = retryInfo.retryDelay.match(/^([\d.]+)s$/);
-                    if (match && match[1]) {
-                        const retrySeconds = parseFloat(match[1]);
-                        if (!isNaN(retrySeconds) && retrySeconds > 0) {
-                            const retryAfterSec = Math.ceil(retrySeconds).toString();
-                            const retryAfterMs = Math.ceil(retrySeconds * 1000).toString();
-                            headers.set('Retry-After', retryAfterSec);
-                            headers.set('retry-after-ms', retryAfterMs);
-                        }
-                    }
-                }
-            }
         }
         const init = {
             status: response.status,
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/rotation.js b/node_modules/opencode-ag-auth/dist/src/plugin/rotation.js
index ee2470b..13b4d48 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/rotation.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/rotation.js
@@ -44,14 +44,26 @@ export class HealthScoreTracker {
     /**
      * Record a successful request - improves health score.
      */
-    recordSuccess(accountIndex) {
+    recordSuccess(accountIndex, latencyMs) {
         const now = Date.now();
         const current = this.getScore(accountIndex);
+        const state = this.scores.get(accountIndex);
+        
+        let newAvgLatency = state?.avgLatencyMs ?? latencyMs ?? 2000;
+        if (latencyMs !== undefined) {
+            newAvgLatency = (newAvgLatency * 0.8) + (latencyMs * 0.2); // EMA
+        }
+
+        let newSuccessRate = state?.successRate ?? 1.0;
+        newSuccessRate = (newSuccessRate * 0.9) + (1.0 * 0.1);
+
         this.scores.set(accountIndex, {
             score: Math.min(this.config.maxScore, current + this.config.successReward),
             lastUpdated: now,
             lastSuccess: now,
             consecutiveFailures: 0,
+            avgLatencyMs: newAvgLatency,
+            successRate: newSuccessRate
         });
     }
     /**
@@ -61,11 +73,17 @@ export class HealthScoreTracker {
         const now = Date.now();
         const state = this.scores.get(accountIndex);
         const current = this.getScore(accountIndex);
+
+        let newSuccessRate = state?.successRate ?? 1.0;
+        newSuccessRate = (newSuccessRate * 0.9) + (0.5 * 0.1); // Rate limit is a partial failure in terms of routing health
+
         this.scores.set(accountIndex, {
             score: Math.max(0, current + this.config.rateLimitPenalty),
             lastUpdated: now,
             lastSuccess: state?.lastSuccess ?? 0,
             consecutiveFailures: (state?.consecutiveFailures ?? 0) + 1,
+            avgLatencyMs: state?.avgLatencyMs ?? 2000,
+            successRate: newSuccessRate
         });
     }
     /**
@@ -75,11 +93,17 @@ export class HealthScoreTracker {
         const now = Date.now();
         const state = this.scores.get(accountIndex);
         const current = this.getScore(accountIndex);
+
+        let newSuccessRate = state?.successRate ?? 1.0;
+        newSuccessRate = (newSuccessRate * 0.9) + (0.0 * 0.1);
+
         this.scores.set(accountIndex, {
             score: Math.max(0, current + this.config.failurePenalty),
             lastUpdated: now,
             lastSuccess: state?.lastSuccess ?? 0,
             consecutiveFailures: (state?.consecutiveFailures ?? 0) + 1,
+            avgLatencyMs: state?.avgLatencyMs ?? 2000,
+            successRate: newSuccessRate
         });
     }
     /**
@@ -105,15 +129,56 @@ export class HealthScoreTracker {
      */
     getSnapshot() {
         const result = new Map();
-        for (const [index] of this.scores) {
+        for (const [index, state] of this.scores) {
             result.set(index, {
                 score: this.getScore(index),
                 consecutiveFailures: this.getConsecutiveFailures(index),
+                successRate: state.successRate,
+                avgLatencyMs: state.avgLatencyMs
             });
         }
         return result;
     }
 }
+
+// ============================================================================
+// ENDPOINT CIRCUIT BREAKER
+// ============================================================================
+export class EndpointCircuitBreaker {
+    endpoints = new Map();
+
+    recordSuccess(endpoint) {
+        this.endpoints.set(endpoint, { state: "CLOSED", failures: 0, nextTrial: 0 });
+    }
+
+    recordFailure(endpoint) {
+        const state = this.endpoints.get(endpoint) || { state: "CLOSED", failures: 0, nextTrial: 0 };
+        state.failures++;
+        if (state.failures >= 3) {
+            state.state = "OPEN";
+            // 30s cooldown before next trial
+            state.nextTrial = Date.now() + 30000;
+        }
+        this.endpoints.set(endpoint, state);
+    }
+
+    canUseEndpoint(endpoint) {
+        const state = this.endpoints.get(endpoint);
+        if (!state) return true;
+        if (state.state === "OPEN") {
+            if (Date.now() > state.nextTrial) {
+                state.state = "HALF_OPEN";
+                return true;
+            }
+            return false;
+        }
+        return true;
+    }
+}
+const globalCircuitBreaker = new EndpointCircuitBreaker();
+export function getCircuitBreaker() {
+    return globalCircuitBreaker;
+}
 // ============================================================================
 // JITTER UTILITIES
 // ============================================================================
@@ -226,7 +291,14 @@ function calculateHybridScore(account, maxTokens) {
     const tokenComponent = (account.tokens / maxTokens) * 100 * 5; // 0-500
     const secondsSinceUsed = (Date.now() - account.lastUsed) / 1000;
     const freshnessComponent = Math.min(secondsSinceUsed, 3600) * 0.1; // 0-360
-    return Math.max(0, healthComponent + tokenComponent + freshnessComponent);
+    
+    // Adaptive scoring: weight by success rate and latency
+    const successRate = account.successRate ?? 1.0;
+    const latencyComponent = Math.max(0, 10000 - (account.avgLatencyMs ?? 2000)) / 10000; // 0 to 1 based on latency (max 10s)
+    
+    // Base score is weighted by successRate and latency (small boost for low latency)
+    const baseScore = Math.max(0, healthComponent + tokenComponent + freshnessComponent);
+    return baseScore * successRate * (0.8 + (latencyComponent * 0.2));
 }
 export const DEFAULT_TOKEN_BUCKET_CONFIG = {
     maxTokens: 50,
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/server.js b/node_modules/opencode-ag-auth/dist/src/plugin/server.js
index 7cb024c..88d288b 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/server.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/server.js
@@ -1,6 +1,9 @@
 import { createServer } from "node:http";
 import { readFileSync, existsSync } from "node:fs";
 import { ANTIGRAVITY_REDIRECT_URI } from "../constants";
+import { createLogger } from "./logger";
+
+const log = createLogger("server");
 const redirectUri = new URL(ANTIGRAVITY_REDIRECT_URI);
 const callbackPath = redirectUri.pathname || "/";
 /**
@@ -90,16 +93,22 @@ function getBindAddress() {
     // Allow user override via environment variable
     const envBind = process.env.OPENCODE_ANTIGRAVITY_OAUTH_BIND;
     if (envBind) {
+        if (envBind === "0.0.0.0") {
+            log.warn("WARNING: Binding OAuth listener to 0.0.0.0 exposes the callback server to your local network.");
+        }
         return envBind;
     }
-    // OrbStack Docker needs 127.0.0.1 for --network host port forwarding
-    if (isOrbStackDockerHost()) {
-        return "127.0.0.1";
+    // Opt-in for binding to all interfaces
+    if (process.env.OPENCODE_ANTIGRAVITY_BIND_ALL === "1" || process.env.OPENCODE_ANTIGRAVITY_BIND_ALL === "true") {
+        log.warn("WARNING: OPENCODE_ANTIGRAVITY_BIND_ALL is enabled. Binding OAuth listener to 0.0.0.0.");
+        return "0.0.0.0";
     }
-    // WSL and remote environments need 0.0.0.0 to be reachable
+    
+    // Warn if we detect a remote environment but default to secure 127.0.0.1
     if (isWSL() || isRemoteEnvironment()) {
-        return "0.0.0.0";
+        log.warn("Remote environment detected. If OAuth callback fails, you may need to set OPENCODE_ANTIGRAVITY_BIND_ALL=1 to allow cross-network access.");
     }
+    
     // Default to 127.0.0.1 for security (local-only access)
     return "127.0.0.1";
 }
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/token.js b/node_modules/opencode-ag-auth/dist/src/plugin/token.js
index 56e0075..7dd2b66 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/token.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/token.js
@@ -63,18 +63,27 @@ export async function refreshAccessToken(auth, client, providerId) {
     }
     try {
         const startTime = Date.now();
-        const response = await fetch("https://oauth2.googleapis.com/token", {
-            method: "POST",
-            headers: {
-                "Content-Type": "application/x-www-form-urlencoded",
-            },
-            body: new URLSearchParams({
-                grant_type: "refresh_token",
-                refresh_token: parts.refreshToken,
-                client_id: ANTIGRAVITY_CLIENT_ID,
-                client_secret: ANTIGRAVITY_CLIENT_SECRET,
-            }),
-        });
+        const controller = new AbortController();
+        const timeoutId = setTimeout(() => controller.abort(), 15000); // 15 seconds timeout
+        
+        let response;
+        try {
+            response = await fetch("https://oauth2.googleapis.com/token", {
+                method: "POST",
+                headers: {
+                    "Content-Type": "application/x-www-form-urlencoded",
+                },
+                body: new URLSearchParams({
+                    grant_type: "refresh_token",
+                    refresh_token: parts.refreshToken,
+                    client_id: ANTIGRAVITY_CLIENT_ID,
+                    client_secret: ANTIGRAVITY_CLIENT_SECRET,
+                }),
+                signal: controller.signal,
+            });
+        } finally {
+            clearTimeout(timeoutId);
+        }
         if (!response.ok) {
             let errorText;
             try {
diff --git a/node_modules/opencode-ag-auth/dist/src/plugin/transform/model-resolver.js b/node_modules/opencode-ag-auth/dist/src/plugin/transform/model-resolver.js
index 8f71afc..5962fff 100644
--- a/node_modules/opencode-ag-auth/dist/src/plugin/transform/model-resolver.js
+++ b/node_modules/opencode-ag-auth/dist/src/plugin/transform/model-resolver.js
@@ -179,6 +179,7 @@ function isPureGeminiTextModel(modelWithoutQuota) {
  * Examples:
  * - "gemini-2.5-flash" → { quotaPreference: "antigravity" }
  * - "gemini-3.1-pro-preview" → { quotaPreference: "antigravity" }
+ * - "gemini-3.1-pro-high" → { quotaPreference: "antigravity" }
  * - "antigravity-gemini-3.1-pro-high" → { quotaPreference: "antigravity", explicitQuota: true }
  * - "claude-sonnet-4-5-thinking-medium" → { quotaPreference: "antigravity" }
  *
